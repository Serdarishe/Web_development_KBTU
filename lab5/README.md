# Lab 5 — Online Store (Angular)

## Запуск проекта

```bash
cd lab5/online-store
npm install
ng serve
```

Открыть в браузере: http://localhost:4200

## Структура компонентов

```
AppComponent
  └─ ProductListComponent   (получает products через @Input)
       └─ ProductCardComponent  (получает product через @Input, эмитит deleteProduct)
```

## Что реализовано

- **4 категории**: Смартфоны, Ноутбуки, Наушники, Планшеты — по 5 товаров каждая (20 всего)
- **Выбор категории** — клик по вкладке/сайдбару показывает товары категории
- **Приветственный экран** — при старте показывается, пока категория не выбрана
- **Поиск** — по названию товара внутри выбранной категории
- **Фильтры** — по цене и рейтингу (корректно работают вместе)
- **Сортировка** — по популярности, цене (возр/убыв), рейтингу
- **Like** — кнопка ❤️ увеличивает счётчик лайков на карточке
- **Delete** — кнопка 🗑 удаляет товар из списка (с подтверждением)
- **Share** — кнопки поделиться в WhatsApp и Telegram (из Lab 4)
- **Галерея** — lightbox при клике на изображение (из Lab 4)
- Все товары ссылаются на реальные позиции на kaspi.kz
